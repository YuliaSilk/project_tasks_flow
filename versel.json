{
  "version": 2,
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/static-build",
      "config": {
        "output": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}