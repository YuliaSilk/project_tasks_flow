{
  "version": 2,
  "builds": [
    {
      "src": "src/**/*",
      "use": "@vercel/static-build",
      "config": {
        "output": "public"
      }
    }
  ],
  "routes": [
    {
      "src": "src/(.*)",
      "dest": "src/$1"
    }
  ]
}