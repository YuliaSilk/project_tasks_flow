{
  "version": 2,
  "builds": [
    {
      "src": "app.ts",
      "use": "@vercel/static-build",
      "config": {
        "output": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}