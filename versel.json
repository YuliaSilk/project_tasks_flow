{
  "version": 2,
  "builds": [
    {
      "src": "src/**/*",
      "use": "@vercel/static-build",
      "config": {
        "output": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "src/(.*)",
      "dest": "src/$1"
    }
  ]
}